# Reactor (server-side only — never exposed to the browser)
REACTOR_API_KEY=rk_...

# Director LLM — first key found wins: Anthropic, DeepSeek, Gemini.
# DIRECTOR_PROVIDER forces one (anthropic | deepseek | gemini | mock).
# Anthropic path uses structured outputs (strongest JSON guarantee);
# mock = canned beats for pipeline tests, never for judges.
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
DIRECTOR_PROVIDER=

# Director model. Defaults: claude-haiku-4-5 / deepseek-chat / gemini-3.5-flash.
DIRECTOR_MODEL=

# Runware — only needed for scripts/gen_refs.js (starting-frame images)
RUNWARE_API_KEY=

# true  = NPC dialogue is spoken inside the generated clip (FastH3 supports it)
# false = clips are ambient-only; the browser speaks NPC lines via speechSynthesis
DIALOGUE_IN_CLIP=true

# true = prefer cached clips over live generation (the demo parachute)
DEMO_MODE=false

PORT=3000
